/** Visual constants for the force graph. */
export const GRAPH_BACKGROUND = "#0e1014";

/** Base node radius (graph units) before zoom scaling. */
export const NODE_RADIUS = 6;

/** Default node fill when no table coloring is applied. */
export const NODE_COLOR = "#8b7cff";

/** Node fill for the currently selected guest. */
export const NODE_SELECTED_COLOR = "#ffffff";

/** Glow radius around nodes (Obsidian-style bloom). */
export const NODE_GLOW = 14;

export const LABEL_FONT = "600 4px Inter, sans-serif";
export const LABEL_COLOR = "rgba(232, 236, 243, 0.92)";

/** Keep-apart edges render in this warning color, dashed. */
export const CONFLICT_COLOR = "rgba(255, 93, 108, 0.9)";
